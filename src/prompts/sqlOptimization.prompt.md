# SQL Optimization Prompt

Analyze the query plan, predicates, joins, sort operations, and returned row estimates. Suggest index changes only when the expected read benefit justifies write and storage cost.

Never recommend a production index without validation in staging.

